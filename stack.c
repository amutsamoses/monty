#include "monty.h"

/**
 * f_stack - prints the top element of the stack
 *
 * @head: head of the stack
 * @counter: line_number counts
 *
 * Return: NULL
*/

void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
